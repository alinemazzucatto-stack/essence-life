import './Agenda.css';
import { useEffect, useMemo, useRef, useState } from 'react';
import { createId as id, today } from '../../shared/app-utils';
import { removeAppReminder, requestReminderPermission, syncDatedReminder } from '../../shared/reminders';
import { readStoredArray } from '../../shared/storage';
import TaskCreateModal from '../home/TaskCreateModal';

type AgendaKind = 'task' | 'habit' | 'event';
type Task = { id:string; title:string; date:string; time?:string; category?:string; priority:string; notes?:string; reminder?:string; recurrence?:string; recurrenceEnd?:string; seriesId?:string; kind?:AgendaKind; duration?:string; done:boolean };
const cats = ['Pessoal','Trabalho','Casa','Saúde','Estudos','Outro'];
const categoryClass = (category?:string) => { const normalized=(category||'Outro').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim(); return ({pessoal:'category-personal',trabalho:'category-work',casa:'category-home',saude:'category-health',estudos:'category-study',outro:'category-other'} as Record<string,string>)[normalized]||'category-other'; };
const kinds: Record<AgendaKind, {label:string; icon:string}> = { task:{label:'Tarefa',icon:'✓'}, habit:{label:'Hábito',icon:'↻'}, event:{label:'Compromisso',icon:'◷'} };
const isoDate = (value:Date) => [value.getFullYear(),String(value.getMonth()+1).padStart(2,'0'),String(value.getDate()).padStart(2,'0')].join('-');
const dateLabel = (value:string) => { const label=new Intl.DateTimeFormat('pt-BR',{weekday:'long',day:'numeric',month:'long'}).format(new Date(value+'T12:00:00')); return label.charAt(0).toUpperCase()+label.slice(1); };
const shortLabel = (value:string) => new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'short'}).format(new Date(value+'T12:00:00'));
const monthDays = (value:string) => { const base = new Date(value+'T12:00:00'); return Array.from({length:new Date(base.getFullYear(),base.getMonth()+1,0).getDate()},(_,index)=>isoDate(new Date(base.getFullYear(),base.getMonth(),index+1,12))); };
const weekFor = (value:string) => { const base = new Date(value+'T12:00:00'); base.setDate(base.getDate()-((base.getDay()+6)%7)); return Array.from({length:7},(_,index)=>{ const current = new Date(base); current.setDate(base.getDate()+index); return isoDate(current); }); };
const recurringDates = (start:string, repeat:string, end:string) => { if(!repeat) return [start]; const base = new Date(start+'T12:00:00'), limit=end?new Date(end+'T12:00:00'):null, max=repeat==='daily'?90:repeat==='weekly'?52:12; const dates=[start]; for(let index=1; index<=max; index++){ const next=new Date(base); if(repeat==='monthly') next.setMonth(base.getMonth()+index); else next.setDate(base.getDate()+index*(repeat==='weekly'?7:1)); if(limit && next>limit) break; dates.push(isoDate(next)); } return dates; };

export default function Agenda(){
  const [tasks,setTasks] = useState<Task[]>(()=>readStoredArray<Task>('essence:tasks'));
  const [selectedDate,setSelectedDate] = useState(today());
  const [view,setView] = useState<'day'|'week'|'month'>('day');
  const [formOpen,setFormOpen] = useState(false);
  const [presetTime,setPresetTime] = useState('');
  const [editing,setEditing] = useState<Task|null>(null);
  const [editScope,setEditScope] = useState<'one'|'future'>('one');
  const [message,setMessage] = useState('');
  const [formError,setFormError] = useState('');
  const [categoryFilter,setCategoryFilter] = useState('Todas');
  const formRef = useRef<HTMLFormElement>(null);

  useEffect(()=>localStorage.setItem('essence:tasks',JSON.stringify(tasks)),[tasks]);
  useEffect(()=>{if(!formOpen)return;const onKey=(event:KeyboardEvent)=>{if(event.key==='Escape')closeForm()};window.addEventListener('keydown',onKey);return()=>window.removeEventListener('keydown',onKey)},[formOpen]);
  const ordered = useMemo(()=>[...tasks].sort((a,b)=>(a.date+(a.time||'23:59')).localeCompare(b.date+(b.time||'23:59'))),[tasks]);
  const selectedWeek = weekFor(selectedDate);
  const selectedMonth = monthDays(selectedDate);
  const visibleTasks = categoryFilter==='Todas'?ordered:ordered.filter(task=>task.category===categoryFilter);
  const selectedTasks = visibleTasks.filter(task=>task.date===selectedDate);
  const unscheduled = selectedTasks.filter(task=>!task.time);
  const weekTasks = visibleTasks.filter(task=>selectedWeek.includes(task.date));
  const monthTasks = visibleTasks.filter(task=>selectedMonth.includes(task.date));
  const todayTasks = tasks.filter(task=>task.date===today());
  const completed = todayTasks.filter(task=>task.done).length;
  const next = ordered.find(task=>!task.done && task.date>=today());
  const notify = (text:string) => { setMessage(text); window.setTimeout(()=>setMessage(''),3200); };
  const openCreate = (time?:string) => { setEditing(null); setFormError(''); setPresetTime(time||''); setFormOpen(true); window.setTimeout(()=>formRef.current?.querySelector<HTMLInputElement>('input[name="title"]')?.focus(),80); };
  const closeForm = () => { setFormOpen(false); setEditing(null); setFormError(''); setPresetTime(''); };
  const toggleTask = (task:Task) => { const done=!task.done; setTasks(all=>all.map(item=>item.id===task.id?{...item,done}:item)); if(done) removeAppReminder(task.id); else if(task.reminder!==undefined && task.reminder!=='' && task.time) syncDatedReminder(task.id,`${kinds[task.kind||'event'].label}: ${task.title}`,task.date,task.time,Number(task.reminder)); };
  const submit = (event:React.FormEvent<HTMLFormElement>) => { event.preventDefault(); setFormError(''); const data=new FormData(event.currentTarget), title=String(data.get('title')).trim(); if(!title){setFormError('Informe o título.'); return;} const repeat=String(data.get('recurrence')), recurrenceEnd=String(data.get('recurrenceEnd')); const valuePreview={date:String(data.get('date')),time:String(data.get('time')),title};if(valuePreview.time&&tasks.some(item=>item.id!==editing?.id&&item.date===valuePreview.date&&item.time===valuePreview.time&&!item.done)){setFormError('Já existe um item pendente neste horário. Revise o conflito ou escolha outro horário.');return;} const value={title,date:String(data.get('date')),time:String(data.get('time')),kind:String(data.get('kind')) as AgendaKind,category:String(data.get('category')),priority:String(data.get('priority')),notes:String(data.get('notes')),reminder:String(data.get('reminder')),recurrence:repeat,recurrenceEnd}; if(value.reminder && !value.time){setFormError('Defina um horário para ativar o lembrete.');return;} if(editing){ const applyFuture=editScope==='future'&&!!editing.seriesId; if(applyFuture){ const {date:_d,...seriesValue}=value; setTasks(all=>all.map(item=>item.seriesId===editing.seriesId&&item.date>=editing.date?{...item,...seriesValue}:item)); tasks.filter(item=>item.seriesId===editing.seriesId&&item.date>=editing.date).forEach(item=>{if(value.reminder!==''&&value.time){syncDatedReminder(item.id,`${kinds[value.kind].label}: ${title}`,item.date,value.time,Number(value.reminder))}else removeAppReminder(item.id)}); if(value.reminder!==''&&value.time) void requestReminderPermission(); notify('Este e os próximos itens da série foram atualizados.'); } else { setTasks(all=>all.map(item=>item.id===editing.id?{...item,...value}:item)); if(value.reminder!==''&&value.time){syncDatedReminder(editing.id,`${kinds[value.kind].label}: ${title}`,value.date,value.time,Number(value.reminder)); void requestReminderPermission();}else removeAppReminder(editing.id); notify('Item atualizado.'); } } else { const dates=recurringDates(value.date,repeat,recurrenceEnd), seriesId=repeat?id():undefined, created=dates.map(date=>({id:id(),...value,date,seriesId,done:false})); setTasks(all=>[...created,...all]); created.forEach(item=>{if(item.reminder!==''&&item.time)syncDatedReminder(item.id,`${kinds[item.kind].label}: ${item.title}`,item.date,item.time,Number(item.reminder))}); if(value.reminder!==''&&value.time) void requestReminderPermission(); notify(repeat?`${dates.length} itens adicionados.`:'Item adicionado à agenda.'); } setSelectedDate(value.date); closeForm(); };
  const moveTask=(taskId:string,date:string,time:string)=>{setTasks(all=>all.map(item=>item.id===taskId?{...item,date,time}:item));notify('Horário atualizado na agenda.');}; const dateStep=(amount:number)=>{ const value=new Date(selectedDate+'T12:00:00'); value.setDate(value.getDate()+amount); setSelectedDate(isoDate(value)); };

  return <section className={'agenda agenda-v3'+(!editing&&formOpen?' agenda-create-new':'')}>

    {formOpen&&!editing&&<TaskCreateModal kind="task" onClose={closeForm} setTasks={setTasks} setHabits={(()=>undefined) as any} onNotice={notify}/>} 

    <article className="card agenda-v3-board">
      <div className="agenda-week-strip" aria-label="Dias da semana">
        {weekFor(selectedDate).map(date=>{const d=new Date(date+'T12:00:00'); const weekday=new Intl.DateTimeFormat('pt-BR',{weekday:'short'}).format(d).replace('.','').slice(0,3).toUpperCase(); return <button type="button" key={date} className={date===selectedDate?'selected':''} onClick={()=>{setSelectedDate(date);setView('day')}}><small>{weekday}</small><b>{d.getDate()}</b><i aria-hidden="true"/></button>})}
      </div>
      <div className="agenda-v3-toolbar">
        <div className="agenda-view-switch" role="tablist" aria-label="Visualização da agenda">
          {([['day','Dia'],['week','Semana'],['month','Mês']] as const).map(([value,label])=><button type="button" role="tab" aria-selected={view===value} key={value} className={view===value?'selected':''} onClick={()=>setView(value)}>{label}</button>)}
        </div>
        <label className="agenda-category-filter">Filtrar categoria<select value={categoryFilter} onChange={event=>setCategoryFilter(event.target.value)} aria-label="Filtrar compromissos por categoria"><option>Todas</option>{cats.map(category=><option key={category}>{category}</option>)}</select></label>

      </div>
      {view==='day' && <div className="agenda-day-view">
        <div className="agenda-date-nav"><button type="button" onClick={()=>dateStep(-1)} aria-label="Dia anterior">‹</button><b>{selectedDate===today()?`Hoje, ${new Intl.DateTimeFormat("pt-BR",{day:"numeric",month:"long"}).format(new Date(selectedDate+"T12:00:00"))}`:dateLabel(selectedDate)}</b><button type="button" onClick={()=>dateStep(1)} aria-label="Próximo dia">›</button></div>
        {unscheduled.length>0&&<div className="agenda-unscheduled-row"><small>Sem horário</small>{unscheduled.map(task=><button type="button" key={task.id} onClick={()=>{setEditing(task);setEditScope('one');setFormError('');setFormOpen(true)}}>{kinds[task.kind||"event"].icon} {task.title}</button>)}</div>}
        <div className="agenda-timeline">
          {Array.from({length:18},(_,index)=>{const hour=String(index+6).padStart(2,'0')+':00'; const entries=selectedTasks.filter(task=>(task.time||'').slice(0,2)===String(index+6).padStart(2,'0')); return <div className="agenda-time-slot" key={hour} onDragOver={event=>event.preventDefault()} onDrop={event=>{event.preventDefault();const taskId=event.dataTransfer.getData("text/plain");if(taskId)moveTask(taskId,selectedDate,hour)}}><time>{hour}</time>{entries.length?<div>{entries.map(task=><article draggable onDragStart={event=>event.dataTransfer.setData('text/plain',task.id)} className={'agenda-timeline-entry '+(task.done?'is-done ':'')+(task.kind||'event')+' '+categoryClass(task.category)} key={task.id}><button type="button" className="agenda-round-check" aria-label={task.done?'Reabrir '+task.title:'Concluir '+task.title} onClick={()=>toggleTask(task)}>{task.done?'✓':''}</button><div role="button" tabIndex={0} className="agenda-timeline-entry-edit" onClick={()=>{setEditing(task);setEditScope('one');setFormError('');setFormOpen(true)}} onKeyDown={event=>{if(event.key==='Enter'){setEditing(task);setEditScope('one');setFormError('');setFormOpen(true)}}}><span>{(task.category||'Pessoal').toUpperCase()}</span><b className="agenda-task-title">{task.title}</b>{task.notes&&<small>{task.notes}</small>}</div><time className="agenda-task-time">{task.time}{task.duration&&<small> · {task.duration} min</small>}</time></article>)}</div>:<button type="button" className="agenda-slot-empty" onClick={()=>openCreate(hour)} aria-label={"Adicionar compromisso às "+hour}/>}</div>})}
        </div></div>}
      {view==='week' && <div className="agenda-week-view"><div className="agenda-date-nav"><button type="button" onClick={()=>dateStep(-7)} aria-label="Semana anterior">‹</button><b>{shortLabel(selectedWeek[0])} — {shortLabel(selectedWeek[6])}</b><button type="button" onClick={()=>dateStep(7)} aria-label="Próxima semana">›</button></div><div className="agenda-week-grid">{selectedWeek.map(date=>{const entries=weekTasks.filter(task=>task.date===date); const day=new Date(date+'T12:00:00'); const weekday=new Intl.DateTimeFormat('pt-BR',{weekday:'long'}).format(day); return <section key={date} className={'agenda-week-day '+(date===selectedDate?'selected':'')}><button type="button" className="agenda-week-day-head" onClick={()=>{setSelectedDate(date);setView('day')}}><small>{weekday}</small><b>{day.getDate()}</b><span>{entries.length?`${entries.length} ${entries.length===1?'compromisso':'compromissos'}`:'Livre'}</span></button><div className="agenda-week-items">{entries.length?entries.map(task=><button type="button" key={task.id} className={'agenda-week-item '+categoryClass(task.category)} onClick={()=>{setSelectedDate(date);setView('day')}}><strong>{task.title}</strong><small>{task.time||'Sem horário'}</small></button>):<span className="agenda-week-empty">Nenhum compromisso</span>}</div></section>})}</div></div>}
      {view==='month' && <div className="agenda-month-view"><div className="agenda-date-nav"><button type="button" onClick={()=>dateStep(-31)} aria-label="Mês anterior">‹</button><b>{new Intl.DateTimeFormat('pt-BR',{month:'long',year:'numeric'}).format(new Date(selectedDate+'T12:00:00'))}</b><button type="button" onClick={()=>dateStep(31)} aria-label="Próximo mês">›</button></div><div className="agenda-month-grid">{['Seg','Ter','Qua','Qui','Sex','Sáb','Dom'].map(day=><small key={day}>{day}</small>)}{Array.from({length:(new Date(selectedMonth[0]+'T12:00:00').getDay()+6)%7},(_,index)=><span className="agenda-month-blank" key={'blank-'+index}/>)}{selectedMonth.map(date=>{const entries=monthTasks.filter(task=>task.date===date); const isToday=date===today(); return <button type="button" key={date} aria-label={`${date}, ${entries.length} compromissos`} className={(date===selectedDate?'selected ':'')+(isToday?'today':'')} onClick={()=>{setSelectedDate(date);setView('day')}}><b>{Number(date.slice(-2))}</b>{entries.length>0&&<span className="agenda-month-dots">{entries.slice(0,3).map(task=><i className={categoryClass(task.category)} key={task.id}/>)}</span>}{entries.length>3&&<em>+{entries.length-3}</em>}</button>})}</div></div>}
    </article>
    <div className="agenda-v3-summary"><article className="card"><small>PROGRESSO DE HOJE</small><strong>{todayTasks.length?`${completed}/${todayTasks.length}`:'Livre'}</strong><p>{todayTasks.length?'ações concluídas':'Sem pendências hoje'}</p></article><article className="card"><small>PRÓXIMO PASSO</small><strong>{next?.title||'Seu tempo, seu ritmo'}</strong><p>{next?(next.date===today()?'Hoje':'Em '+shortLabel(next.date))+(next.time?' · '+next.time:''):'Adicione algo quando fizer sentido.'}</p></article></div>
    {formOpen&&<div className="agenda-modal-backdrop" onClick={closeForm}><article className="card agenda-v3-form" role="dialog" aria-modal="true" aria-labelledby="agenda-form-title" onClick={event=>event.stopPropagation()}><div className="card-head"><div><small>{editing?'EDITAR ITEM':'NOVO ITEM'}</small><h3 id="agenda-form-title">{editing?'Atualize os detalhes':'Organize um momento'}</h3></div><button type="button" onClick={closeForm}>Fechar</button></div><form ref={formRef} className="form" onSubmit={submit}><label className="wide">Título<input name="title" defaultValue={editing?.title||''} placeholder="Ex.: Consulta, estudar ou caminhar" required/></label><label>Tipo<select name="kind" defaultValue={editing?.kind||'event'}><option value="event">Compromisso</option><option value="task">Tarefa</option><option value="habit">Hábito</option></select></label><label>Categoria<select name="category" defaultValue={editing?.category||'Pessoal'}>{cats.map(value=><option key={value}>{value}</option>)}</select></label><label>Data<input name="date" type="date" defaultValue={editing?.date||selectedDate} required/></label><label>Horário<input name="time" type="time" defaultValue={editing?.time||presetTime||''}/></label><label>Duração<select name="duration" defaultValue={editing?.duration||""}><option value="">Sem duração</option><option value="15">15 min</option><option value="30">30 min</option><option value="45">45 min</option><option value="60">1 hora</option><option value="90">1h30</option><option value="120">2 horas</option></select></label><label>Lembrete<select name="reminder" defaultValue={editing?.reminder||''}><option value="">Sem lembrete</option><option value="0">No horário</option><option value="15">15 min antes</option><option value="30">30 min antes</option><option value="60">1 hora antes</option></select></label><label>Repetir<select name="recurrence" defaultValue={editing?.recurrence||''}><option value="">Não repetir</option><option value="daily">Diariamente</option><option value="weekly">Semanalmente</option><option value="monthly">Mensalmente</option></select></label><label>Repetir até<input name="recurrenceEnd" type="date" min={editing?.date||selectedDate} defaultValue={editing?.recurrenceEnd||''}/></label><label>Prioridade<select name="priority" defaultValue={editing?.priority||'Normal'}><option>Normal</option><option>Alta</option><option>Baixa</option></select></label>{editing?.seriesId&&<fieldset className="wide agenda-edit-scope"><legend>Este item faz parte de uma série recorrente. Aplicar alterações a:</legend><label><input type="radio" name="editScope" checked={editScope==='one'} onChange={()=>setEditScope('one')}/> Somente este item</label><label><input type="radio" name="editScope" checked={editScope==='future'} onChange={()=>setEditScope('future')}/> Este e os próximos</label></fieldset>}<label className="wide">Observação<input name="notes" defaultValue={editing?.notes||''} placeholder="Local ou um detalhe importante"/></label>{formError&&<p className="agenda-form-error wide" role="alert">{formError}</p>}<button className="primary wide">{editing?'Salvar alterações':'Adicionar à agenda'}</button></form></article></div>}
    {message&&<div className="agenda-toast" role="status">✓ {message}</div>}
    <button type="button" className="agenda-floating-add" onClick={()=>openCreate()} aria-label="Adicionar novo compromisso">+</button>
  </section>;
}
